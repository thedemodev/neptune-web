import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Image } from '@transferwise/components';
import code from '../../liveEditorCode/image.code';

Image component renders an img tag with some optional lazy load feature.

Be aware that lazy load can have a negative impact on SEO as the inital src won't correspond to the initil one.

<LiveEditorBlock code={code} scope={{ Image }} />
<GeneratePropsTable componentName="Image" />
<>
<Image
  alt="test"
  id="id1"
   className={'m-t-5'}
  src="//i.picsum.photos/id/1025/300/200.jpg"
  withLazy
  onLoad={() => console.log('image loaded 1')}
  onError={errorEvent => console.log(errorEvent)}
/>

<Image
  alt="test"
  id="id1"
   className={'m-t-5'}
  src="//i.picsum.photos/id/1025/300/200.jpg"
  withLazy
  onLoad={() => console.log('image loaded 2')}
  onError={errorEvent => console.log(errorEvent)}
/>

<Image
  alt="test"
  id="id1"
  className={'m-t-5'}
  src="//i.picsum.photos/id/1025/300/200.jpg"
  withLazy
  onLoad={() => console.log('image loaded 3')}
  onError={errorEvent => console.log(errorEvent)}
/>
</>

export const meta = {
  name: 'Image',
};
